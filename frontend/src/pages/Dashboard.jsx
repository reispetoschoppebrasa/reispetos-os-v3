import{useEffect,useState}from"react";import{api}from"../services/api";export default function Dashboard(){const[data,setData]=useState(null),[error,setError]=useState("");useEffect(()=>{api("/dashboard").then(setData).catch(e=>setError(e.message))},[]);if(error)return <div className="panel error">{error}</div>;if(!data)return <div className="panel">Carregando dashboard...</div>;const cards=[["Faturamento hoje",`R$ ${data.faturamento_hoje.toFixed(2)}`],["Lucro estimado",`R$ ${data.lucro_estimado.toFixed(2)}`],["Mesas abertas",data.mesas_abertas],["Estoque crítico",data.estoque_critico],["Produtos ativos",data.produtos_ativos],["Valor em estoque",`R$ ${data.valor_estoque.toFixed(2)}`]];return <><div className="pageTitle"><h1>Dashboard</h1><p>Sistema REI'SPETOS</p></div><div className="cards">{cards.map(([l,v])=><div className="card" key={l}><span>{l}</span><b>{v}</b></div>)}</div><div className="panel"><h2>Fundação v3 pronta</h2><p>{data.status}</p><p>A arquitetura agora está separada por páginas, componentes, serviços e rotas de API.</p></div></>}
