import{useState}from"react";import{api}from"../services/api";export default function Login({onLogin}){const[username,setUsername]=useState("admin"),[password,setPassword]=useState("1234"),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setError("");try{const data=await api("/auth/login",{method:"POST",body:JSON.stringify({username,password})});localStorage.setItem("reispetos_token",data.access_token);localStorage.setItem("reispetos_user",JSON.stringify(data.user));onLogin(data.user)}catch(err){setError(err.message)}};return <main className="loginPage"><form className="loginCard" onSubmit={submit}><div className="logo big">R</div><h1>REI'SPETOS OS</h1><p>Gestão operacional v3</p><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="Usuário"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha"/><button>Entrar</button>{error&&<div className="error">{error}</div>}<small>admin / 1234</small></form></main>}
