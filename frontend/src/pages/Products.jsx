import{useEffect,useState}from"react";import{api}from"../services/api";export default function Products(){const[items,setItems]=useState([]),[error,setError]=useState("");useEffect(()=>{api("/products").then(setItems).catch(e=>setError(e.message))},[]);return <><div className="pageTitle"><h1>Produtos</h1><p>Base inicial cadastrada</p></div>{error&&<div className="panel error">{error}</div>}<div className="productGrid">{items.map(p=><article className="productCard" key={p.id}><h3>{p.name}</h3><span>{p.category}</span><div><b>Venda: R$ {p.price.toFixed(2)}</b><small>Custo: R$ {p.cost.toFixed(2)}</small></div><strong>Estoque: {p.stock}</strong></article>)}</div></>}
