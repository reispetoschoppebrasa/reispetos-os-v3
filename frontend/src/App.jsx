import React, { useState } from "react";import Sidebar from"./components/Sidebar";import Login from"./pages/Login";import Dashboard from"./pages/Dashboard";import Products from"./pages/Products";import Placeholder from"./pages/Placeholder";export default function App(){const stored=localStorage.getItem("reispetos_user");const[user,setUser]=useState(stored?JSON.parse(stored):null),[page,setPage]=useState("dashboard");const logout=()=>{localStorage.clear();setUser(null)};if(!user)return <Login onLogin={setUser}/>;const titles={stock:"Estoque",tables:"Mesas e Comandas",production:"Produção",cash:"Caixa",reports:"Relatórios"};return <div className="app"><Sidebar page={page} setPage={setPage} user={user} logout={logout}/><section className="content">{page==="dashboard"&&<Dashboard/>}{page==="products"&&<Products/>}{titles[page]&&<Placeholder title={titles[page]}/>}</section></div>}
